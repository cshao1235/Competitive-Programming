import java.io.BufferedReader;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;
import java.util.StringTokenizer;

public class Mowing {
	/*static class AVLTree {
		
	}
	static class RangeTreeNode {
		
	}
	static class IntervalTreeNode {
		int min;
		int max;
		int med;
		RangeTreeNode currentLeft;
		RangeTreeNode currentRight;
		IntervalTreeNode left;
		IntervalTreeNode right;
		public IntervalTreeNode(int min, int max, int med)
		{
			this.min = min;
			this.max = max;
			this.med = med;
			//current = new RangeTreeNode();
			left = new IntervalTreeNode(min, med, (min + med)/2);
			right = new IntervalTreeNode(med, max, (med + max)/2);
			
		}
		public void addSegment(int x, int start, int end)
		{
			if(end < med)
				left.addSegment(x, start, end);
			else if(start > med)
				right.addSegment(x, start, end);
			else
				currentLeft
		}
	}
	public static void main(String[] args) {
		try {
			BufferedReader f = new BufferedReader(new FileReader("fortmoo.in"));
			StringTokenizer st = new StringTokenizer(f.readLine());
			int n = Integer.parseInt(st.nextToken());
			int t = Integer.parseInt(st.nextToken());
			int[][] positions = new int[n][2];
			for(int i = 0; i < n; i++)
			{
				st = new StringTokenizer(f.readLine());
				positions[i][0] = Integer.parseInt(st.nextToken());
				positions[i][1] = Integer.parseInt(st.nextToken());
			}
			f.close();
			
		}
		catch(IOException e)
		{
			e.printStackTrace();
		}

	}*/

}
